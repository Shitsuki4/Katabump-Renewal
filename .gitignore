# generated at runtime by proxy_handler.py from PROXY_URL secret
config.json

# node topology (may contain private node addresses) — use POOL_JSON secret
pool.json

# runtime probe artifacts
tc_*.json
probe_cfg.json
probe_sb.log
singbox.log

# python
__pycache__/
*.pyc
